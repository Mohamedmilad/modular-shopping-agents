First Of all you need to install the libraries from the requirements.txt 

Go to the Folder ShoppingAgent this is the main agent and the main file so you change directory to it
and run "python agent.py"
two things will work. The UI and the terminal both will show the agents outputs.
But in the middle the agent will ask you to choose the product, this will happen only in the UI.

And finally the agent stores the user history in a json file in data folder.